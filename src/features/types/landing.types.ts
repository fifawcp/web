import { LucideIcon } from "lucide-react";

export type CardConfig = {
  icon: LucideIcon;
  translationKey: string;
  borderColor: string;
  gradientFrom: string;
  gradientTo: string;
  gradientDark: string;
  delay: number;
};
